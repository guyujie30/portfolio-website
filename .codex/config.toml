# Project-level Codex configuration converted from .claude/settings.local.json.
#
# Claude Code permissions:
# - Read / Write
# - Bash(npm *)
# - Bash(git *)
# - Bash(node *)
# - deny Bash(rm -rf *)
#
# Codex does not use Claude Code's JSON allow/deny syntax. Shell command approval
# and destructive-command blocking are handled by Codex approvals, rules, hooks,
# and the global/project AGENTS.md instructions.

[features]
hooks = true

# Equivalent intent for Claude Code's autoCompactThreshold = 80.
# Codex Desktop manages context compaction at runtime; there is no direct
# project config field with the same name.

[[hooks.PostToolUse]]
matcher = "Write|Edit"

[[hooks.PostToolUse.hooks]]
type = "command"
command = "Write-Output 'Files were modified; review the changes before continuing.'"
timeout = 30
